<?xml version="1.0" encoding="UTF-8" ?>
<customControls:ContentViewWithSetPropertyMethod
    x:Class="Ui.MauiX.CustomControls.WorkflowMenu"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:customControls="clr-namespace:Ui.MauiX.CustomControls"
    x:Name="this"
    VerticalOptions="FillAndExpand">
    <ContentView.Content>
        <customControls:TabView
            x:Name="MainTabView"
            ArePagesCached="False"
            BindingContext="{x:Reference this}"
            CustomMenuItems="{Binding CustomMenuItems}"
            SelectedTabChanged="TabView_SelectedTabChanged"
            TabAlignment="{Binding MenuAlignment}" />
    </ContentView.Content>
</customControls:ContentViewWithSetPropertyMethod>