<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="Ui.MauiX.Test.Views.ToggleSupplementalPageControls"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:customControls="clr-namespace:Ui.MauiX.CustomControls;assembly=Ui.MauiX">
    <ContentView.Content>
        <StackLayout Spacing="15">
            <customControls:Setter
                InputMode="AlphaNumberic"
                Label="Toggled Text"
                Value="{Binding ToggledText}" />
            <customControls:Setter
                InputMode="AlphaNumberic"
                Label="Untoggled Text"
                Value="{Binding UntoggledText}" />
            <customControls:Setter
                Label="Toggled Color"
                Style="{StaticResource ColorSetterStyle}"
                Value="{Binding ToggledColor}" />
            <customControls:Setter
                Label="Untoggled Color"
                Style="{StaticResource ColorSetterStyle}"
                Value="{Binding UntoggledColor}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>